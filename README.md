# C++ raylib template

A minimal C++ project using raylib and CMake. CMake downloads raylib automatically,
so no system-wide raylib installation is required.

Uses C++ 23 and Raylib 6.0. 

## Build and run

```sh
cmake -B build
cmake --build build
./build/cpp_raylib_cmake_template
```
